version: '3.8'

services:
  code-editor:
    build: .
    ports:
      - "5001:5001"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - FLASK_ENV=production
    restart: unless-stopped
    volumes:
      - ./sessions:/app/sessions